name: Install Nginx 
hosts: tea-app-server
become: yes

tasks:
  - name: Install Nginx
    package:
      name: nginx
      state: latest 
  - name: Start Nginx
    service:
      name: nginx
      state: started
